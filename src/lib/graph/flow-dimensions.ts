// Shared dimensions keep ELK, React Flow, and the minimap in agreement.
// The cards are intentionally compact enough for bounded real-repository samples.
export const COMMIT_NODE_WIDTH = 196;
export const COMMIT_NODE_HEIGHT = 54;

# Optional. Without a token, public repositories use GitHub's REST API.
# Set only on the server. Never use public-prefixed environment variables.
GITHUB_TOKEN=
# Optional repository to show on first load. The Demo link always stays available.
MULTIVERSE_REPOSITORY=
